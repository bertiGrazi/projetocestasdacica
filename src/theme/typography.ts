export const typography = {
  fontFamily: {
    heading: "var(--font-playfair)",
    body: "var(--font-inter)",
  },

  fontWeight: {
    regular: 400,
    medium: 500,
    semibold: 600,
    bold: 700,
  },
} as const;